# Private key for contract deployment (without 0x prefix or with — both work)
PRIVATE_KEY=your_private_key_here

# EVM JSON-RPC endpoint (Energy Chain testnet node0 default)
RPC_URL=http://localhost:8545
